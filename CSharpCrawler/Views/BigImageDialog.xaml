<Window x:Class="CSharpCrawler.Views.BigImageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CSharpCrawler.Controls"
        mc:Ignorable="d"
        Title="BigImageDialog" Height="720" Width="1280"  ShowInTaskbar="False" WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen" Background="Transparent">
    <Border CornerRadius="10">
        <Border.Background>
            <SolidColorBrush Color="Silver" Opacity=".5"></SolidColorBrush>
        </Border.Background>

        <Canvas Name="canvas">
            <Grid Width="{Binding ElementName=canvas,Path=Width}" Height="{Binding ElementName=canvas,Path=Height}">
                <Image x:Name="bigImage"></Image>
            </Grid>
        </Canvas>
    </Border>
</Window>
